# OpenSpec Configuration — asistencia
# Auto-generated by sdd-init on 2026-09-08
schema: spec-driven

context: |
  Tech stack: Python 3.12, Flask, Peewee ORM, PostgreSQL, pytest
  Architecture: Flask app factory pattern with Peewee models for attendance tracking, shifts, and turns.
  Testing: pytest 7.4+ available; strict TDD mode enabled (strict_tdd: true).
  Style: PEP 8, conventions from firmaDocs reference project.
  Attendance control for work shifts and turns (jornadas de trabajo y turnos).

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify auth-related changes clearly
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing Flask/Peewee patterns
    tdd: true
    test_command: "python -m pytest"
  verify:
    test_command: "python -m pytest"
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas